{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Capability for the local management window only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
